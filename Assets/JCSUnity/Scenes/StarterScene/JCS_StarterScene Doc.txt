===========================================================
* Something u need to do before using this scene.
----------------------------------------------------

1. "JCS_GameSetting->THIS_IS_GAME_SCENE" check or uncheck.

2. [Audio Listener] either on "Player" or on "Camera".
    * Recommanded
        -> if isnt a game scene add [Aduio Listener] on "Camera".
        -> if is a game scene, add [Aduio Listener] either on "Player" or on "Camera" is fine.
